# Pinned for CPU<->GPU reproducibility (PLAN: "Pinned deps + fixed seeds").
# Python >= 3.10.

# --- Agent service ---
# fastapi 0.138 / starlette 1.x keeps the core compatible with the optional MCP
# server (requirements-mcp.txt), so both live in one venv.
fastapi==0.138.0
uvicorn[standard]==0.34.0
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Ollama client ---
ollama==0.4.5

# --- Data + deterministic detection ---
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1

# --- RAG (HW3 patterns): vector store + lexical recall ---
chromadb==0.6.3
rank-bm25==0.2.2

# --- Tests ---
pytest==8.3.4
