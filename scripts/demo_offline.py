"""demo_offline.py — the scripted end-to-end "local advantage" demo.

Runs one flagged lot through detect -> retrieve -> draft -> approve -> commit,
printing the local-agent advantage at each step (data never leaves the machine,
runs air-gapped, no per-token cost, full control). Meant to be run with the
network OFF. STUB — success metric #2.
"""
from __future__ import annotations


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
