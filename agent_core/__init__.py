"""Probe Lot-Disposition Agent — Python core (Architecture A).

Deterministic shell around a probabilistic core: classic code *decides and
executes* (detector + shell); the LLM only *explains and converses* (agent).
See ../PLAN.md and ../docs/HOW_IT_WORKS.md.
"""
